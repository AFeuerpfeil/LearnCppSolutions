#ifndef ADD_H   /* Header guard makes sure, when including this multiple times,
that the next section is only executed once */
#define ADD_H 

int add(int, int);

#endif


/* Simpler alternative
#pragma once 

int add(int, int);
*/
